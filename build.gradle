allprojects {
    version = '1.4.0'

    repositories {
        mavenCentral()
    }
}

tasks.register('install') {
    dependsOn([':backend:build', ':frontend:appNpmTest'])
    description = 'Builds the project and runs all tests'
    group = 'build'
}